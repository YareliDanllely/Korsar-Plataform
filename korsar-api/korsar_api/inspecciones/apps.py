from django.apps import AppConfig


class InspeccionesConfig(AppConfig):
    """
    Definir la configuración de la aplicación Inspecciones
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'inspecciones'
